spring:
  data:
    rest:
      base-path: /api
    mongodb:
      uri: mongodb://user:pass@ds133241.mlab.com:33241/onetests

server:
  port: 3000

logging:
  level:
    ROOT: INFO
    org.springframework: DEBUG